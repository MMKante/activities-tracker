import { createStore } from 'vuex'

const state = {}
const getters = {}
const mutations = {}
const actions = {}

export default createStore({
	state: state,
	getters: getters,
	mutations: mutations,
	actions: actions,
})
